class Solution {
    public int bitwiseComplement(int n) {
        if (n == 0) return 1;

        int count = 0;
        int temp = n;
        while (temp > 0) {
            temp = temp >> 1;
            count++;
        }
        int mask = (1 << count) - 1;

        return n ^ mask;
    }
}
