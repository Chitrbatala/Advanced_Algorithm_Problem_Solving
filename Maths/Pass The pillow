class Solution {
    public int passThePillow(int n, int time) {
        int cycle = (n - 1) * 2;
        int t = time % cycle;

        if (t < n) {
            return 1 + t;
        } else {
            return n - (t - (n - 1));
        }
    }
}
