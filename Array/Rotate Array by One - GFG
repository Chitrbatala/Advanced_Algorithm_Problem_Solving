class Solution {
    public void rotate(int[] arr) {
        int n = arr.length - 1;
        
        int last = arr[n];
        
        for (int i = n - 1; i>= 0; i--) {
            arr[i + 1] = arr[i];
        }
        arr[0] = last;
    }
}
